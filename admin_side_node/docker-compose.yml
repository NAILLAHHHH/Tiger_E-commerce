services:
  postgres:
    image: postgres:16-alpine
    container_name: tiger-strapi-postgres
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: tygamart
      POSTGRES_USER: strapi
      POSTGRES_PASSWORD: strapi
    volumes:
      - strapi_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U strapi -d tygamart"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  strapi_pg_data:
