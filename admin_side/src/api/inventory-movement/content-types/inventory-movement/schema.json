{
  "kind": "collectionType",
  "collectionName": "inventory_movements",
  "info": {
    "singularName": "inventory-movement",
    "pluralName": "inventory-movements",
    "displayName": "Stock movement",
    "description": "Auto-logged record when stock changes manually — restocks, adjustments, imports (sales are tracked via Orders)"
  },
  "options": {
    "draftAndPublish": false
  },
  "attributes": {
    "movement_type": {
      "type": "enumeration",
      "enum": [
        "restock",
        "adjustment",
        "import",
        "initial",
        "count"
      ],
      "required": true,
      "description": "What caused the stock change"
    },
    "quantity_delta": {
      "type": "integer",
      "required": true,
      "description": "Change in units (negative = out, positive = in)"
    },
    "quantity_before": {
      "type": "integer",
      "required": true,
      "min": 0,
      "description": "Stock count before this change"
    },
    "quantity_after": {
      "type": "integer",
      "required": true,
      "min": 0,
      "description": "Stock count after this change"
    },
    "item_code": {
      "type": "string",
      "description": "Product code at time of movement"
    },
    "size": {
      "type": "string",
      "description": "Legacy — prefer options_label"
    },
    "color": {
      "type": "string",
      "description": "Legacy — prefer options_label"
    },
    "options_label": {
      "type": "string",
      "description": "Human-readable options at time of movement, e.g. Size M · Color Black"
    },
    "product_name": {
      "type": "string",
      "description": "Product name at time of movement"
    },
    "reason": {
      "type": "text",
      "description": "Optional note — restock delivery, manual correction, etc."
    },
    "source": {
      "type": "enumeration",
      "enum": ["system", "admin", "import", "api"],
      "default": "system",
      "description": "Where the change came from"
    },
    "product_variant": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::product-variant.product-variant",
      "description": "Which variant row changed"
    }
  }
}
